import { combineReducers } from "redux";
import { Reducer } from "./Reducer";

// Creating the rootReducer function
const rootReducer = combineReducers({
  Reducer,
});

export default rootReducer;
